'use strict';

/**
 * personal-activity-goal router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::personal-activity-goal.personal-activity-goal');
