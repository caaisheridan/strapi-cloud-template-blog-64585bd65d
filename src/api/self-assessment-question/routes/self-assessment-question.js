'use strict';

/**
 * self-assessment-question router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::self-assessment-question.self-assessment-question');
