'use strict';

/**
 * self-assessment-question service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::self-assessment-question.self-assessment-question');
